volumePermissions:
  enabled: true
tls:
  enabled: false
  certificatesSecret: postgres-cert
  certFilename: "tls.crt"
  certKeyFilename: "tls.key"
primary:
  resources:
    requests:
      cpu: 1
      memory: 128Mi
    limits:
      cpu: 3
      memory: 1024Mi
