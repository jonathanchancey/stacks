---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: assets
  namespace: homepage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
