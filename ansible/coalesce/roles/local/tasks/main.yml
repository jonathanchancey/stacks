- name: Update kubeconfig with kube-vip address
  ansible.builtin.copy:
    dest: ./autogenerated/rke2.yaml
    content: "{{ lookup('file', './autogenerated/rke2-localhost.yaml') | regex_replace('127.0.0.1', {{ apiserver_endpoint }}) }}"
    mode: "0600"
    force: yes
