---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-postgres-staging
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: "5Gi"
  storageClassName: "ceph-block"
