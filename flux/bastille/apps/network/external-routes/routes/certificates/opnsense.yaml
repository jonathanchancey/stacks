apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: opnsense-chancey-dev
  namespace: network
spec:
  secretName: opnsense-chancey-dev-tls
  dnsNames:
    - opnsense.chancey.dev
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  privateKey:
    algorithm: RSA
    size: 2048
    rotationPolicy: Always
