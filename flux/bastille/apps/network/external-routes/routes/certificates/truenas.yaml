apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: truenas-chancey-dev
  namespace: network
spec:
  secretName: truenas-chancey-dev-tls
  dnsNames:
    - truenas.chancey.dev
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  privateKey:
    algorithm: RSA
    size: 2048
    rotationPolicy: Always
