---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: &name kanidm-tls
spec:
  dnsNames:
    - idm.chancey.dev
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-production
  privateKey:
    algorithm: RSA
    size: 2048
  secretName: *name
  usages:
    - digital signature
    - key encipherment
