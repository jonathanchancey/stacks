---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: kanidm
  labels:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-block
