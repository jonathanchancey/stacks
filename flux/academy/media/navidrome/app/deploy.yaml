---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: navidrome
  namespace: navidrome
  labels:
    app: navidrome
spec:
  replicas: 1
  progressDeadlineSeconds: 600
  revisionHistoryLimit: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: navidrome
  template:
    metadata:
      labels:
        app: navidrome
    spec:
      containers:
        - name: navidrome
          image: deluan/navidrome@sha256:d753f3f0ba65d838d288e58c2cb1254a519f439b88fa0fa8378178f5e8b17587
          resources:
            requests:
              memory: "100Mi"
              cpu: "100m"
            limits:
              memory: "500Mi"
              cpu: "500m"
          env:
            - name: TZ
              value: America/Los_Angeles
            - name: ND_SCANSCHEDULE
              value: "1h"
            - name: ND_LOGLEVEL
              value: "info"
            - name: ND_SESSIONTIMEOUT
              value: "24h"
          ports:
            - containerPort: 4533
          volumeMounts:
            - mountPath: /music
              name: pv-ladybug-music-smb
            - mountPath: "/data"
              name: db
      volumes:
        - name: db
          persistentVolumeClaim:
            claimName: navidrome-db
        - name: pv-ladybug-music-smb
          persistentVolumeClaim:
            claimName: pv-ladybug-music-smb
